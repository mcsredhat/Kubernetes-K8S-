# ============================================================================
# 07-autoscaling.yaml - Scaling and High Availability
# ============================================================================
# Purpose: Provides automatic scaling and ensures high availability during maintenance
# Dependencies: Deployment must exist, metrics-server required for HPA
# Status: VERIFIED CORRECT - No changes needed
# ============================================================================

# Horizontal Pod Autoscaler - Automatic scaling based on metrics
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: myapp-hpa
  namespace: myapp-prod
  labels:
    app: myapp
    app.kubernetes.io/name: myapp
    app.kubernetes.io/component: autoscaling
    app.kubernetes.io/part-of: myapp
  annotations:
    description: "Horizontal Pod Autoscaler for myapp with CPU and memory metrics"
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: myapp-deployment
  minReplicas: 3
  maxReplicas: 20
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
      - type: Percent
        value: 50
        periodSeconds: 60
      - type: Pods
        value: 2
        periodSeconds: 60
      selectPolicy: Max
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
      - type: Percent
        value: 10
        periodSeconds: 60
      - type: Pods
        value: 1
        periodSeconds: 60
      selectPolicy: Min

---
# Pod Disruption Budget - Ensures availability during maintenance
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: myapp-pdb
  namespace: myapp-prod
  labels:
    app: myapp
    app.kubernetes.io/name: myapp
    app.kubernetes.io/component: availability
    app.kubernetes.io/part-of: myapp
  annotations:
    description: "Pod Disruption Budget to ensure high availability during cluster maintenance"
spec:
  minAvailable: 50%
  selector:
    matchLabels:
      app: myapp
      app.kubernetes.io/name: myapp